# models/helpdesk_ticket_api.py (podría estar vacío por ahora)
from odoo import api, fields, models


class HelpdeskTicketApi(models.TransientModel):
    _name = 'helpdesk.ticket.api'
    _description = 'Modelo temporal para la API de Helpdesk'
